// Generated by Selenium IDE
const { Builder, By, Key, until } = require('selenium-webdriver')
const assert = require('assert')

describe('QD_DK_07', function() {
  this.timeout(60000)
  let driver
  let vars
  beforeEach(async function() {
     driver = await new Builder().forBrowser('MicrosoftEdge').build()
    await driver.manage().window().maximize();
    vars = {}
  })
  afterEach(async function() {
    await driver.quit();
  })
  it('QD_DK_07', async function() {
    await driver.get("https://meowshopp.com/")
    await driver.findElement(By.css(".e-fas-user > path")).click()
    await driver.sleep(3000)
    await driver.findElement(By.linkText("Đăng ký")).click()
    await driver.sleep(3000)
    await driver.findElement(By.id("user_login-580")).sendKeys("mylinh_lam")
    await driver.findElement(By.id("first_name-580")).sendKeys("Lâm")
    await driver.findElement(By.id("last_name-580")).sendKeys("Mỹ Linh")
    await driver.findElement(By.id("user_email-580")).sendKeys()
    await driver.findElement(By.id("user_password-580")).sendKeys()
    await driver.findElement(By.id("confirm_user_password-580")).sendKeys()
    const submitBtn = await driver.findElement(By.id("um-submit-btn"));

    await driver.executeScript(
      "arguments[0].scrollIntoView({block:'center'});",
      submitBtn
    );

    await driver.sleep(1000);

    await driver.executeScript(
      "arguments[0].click();",
      submitBtn
    );
    await driver.sleep(5000)
    {
      const elements = await driver.findElements(By.id("um-error-for-user_email-580"))
      assert(elements.length)
    }
  })
})
