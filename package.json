{
  "name": "meowshop-testing",
  "version": "1.0.0",
  "description": "Selenium + Mocha + Jira CI/CD Testing Framework",
  "main": "index.js",
  "scripts": {
    "test": "mocha selenium-tests/**/*.js --require dotenv/config --require ./test/setup.js",
    "test-all": "mocha selenium-tests/**/*.js --require dotenv/config --require ./test/setup.js"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "chromedriver": "^136.0.0",
    "form-data": "^4.0.0",
    "mocha": "^10.0.0",
    "selenium-webdriver": "^4.0.0",
    "dotenv": "^16.4.0"
  }
}
